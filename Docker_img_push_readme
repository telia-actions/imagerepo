Here's an example of how you can pull a Docker image from Docker Hub and push it to Artifactory using Github Actions:

Create a workflow in your Github Actions workflow file (e.g., .github/workflows/push-to-artifactory.yml).


In the workflow, define a step that uses the Docker CLI to log into Docker Hub:

- name: Login to Docker Hub
  run: |
    echo "${​​​​​​​{​​​​​​​ secrets.DOCKER_PASSWORD }​​​​​​​}​​​​​​​" | docker login -u "${​​​​​​​{​​​​​​​ secrets.DOCKER_USERNAME }​​​​​​​}​​​​​​​" --password-stdin


Define another step that uses the Docker CLI to pull the image:


- name: Pull image
  run: docker pull <image-name>:<tag>



Define another step that uses the JFrog CLI to log into Artifactory:


- name: Login to JFrog Artifactory
  run: jfrog rt config --url=<artifactory-url> --user=<artifactory-username> --password=<artifactory-password>


Finally, define a step that uses the JFrog CLI to push the image to Artifactory:

- name: Push image to Artifactory
  run: jfrog rt docker-push <image-name>:<tag> <artifactory-repository>
